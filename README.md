# Studio V Homepage

Static homepage prototype for Studio V, prepared for GitHub Pages deployment.

## Preview

Open `index.html` directly or serve this directory with a static file server.

```sh
python3 -m http.server 4178
```

## Notes

Large source videos are intentionally excluded from git under `assets/video/source/`.
Use the web-ready assets in `assets/video/` for deployment.
